# HLint configuration file
# https://github.com/ndmitchell/hlint

# eta reduce doesn't work for with-style functions of type (c => r) -> r
- ignore: {name: Eta reduce}
- ignore: {name: Avoid lambda}

# Happening often when defining the id method
- ignore: {name: Use id}